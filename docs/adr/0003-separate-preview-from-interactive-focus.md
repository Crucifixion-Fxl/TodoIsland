# Separate preview from interactive focus

Hovering over the collapsed Island will open a read-only Island Preview without taking focus from the current application. Clicking the Island will instead enter a Pinned Island that can become key and accept keyboard input, preserving lightweight glanceability while still supporting Reminder creation and editing.
